name: tabirdunyasi
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  flutter_widget_from_html_core: ^0.9.0
  flutter_native_splash: ^2.2.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  change_app_package_name: ^1.1.0
  flutter_lints: ^2.0.1
  flutter_launcher_icons: ^0.10.0
  flutter_app_name: ^0.1.1
  flutter_local_notifications: ^9.9.1
  rxdart: ^0.27.5
  intl: ^0.17.0

flutter:
  uses-material-design: true
  assets:
    - assets/

flutter_app_name:
  name: "Rüya Alemi"

flutter_native_splash:
  background_image: assets/bg.png
  image: assets/home1.png
  branding: assets/home1.png

  android_12:
    background_image: assets/bg.png
    image: assets/home1.png

#flutter pub run flutter_native_splash:create

flutter_icons:
  android: "logoicon"
  ios: true
  image_path: "assets/icon.png"
# flutter pub run change_app_package_name:main ruyaalemi.shemaster.com
# flutter pub run flutter_launcher_icons:main
# flutter pub run flutter_app_name

# dart pub global activate flutterfire_cli
# flutterfire configure

# flutter build appbundle --build-name=9.1.0 --build-number=10

# dart fix --dry-run
# dart fix --apply
# flutter format .
# flutter analyze
# flutter upgrade .
# dart migrate.
# flutter pub outdated --mode=null-safety
# flutter pub upgrade --null-safety
# dart migrate
